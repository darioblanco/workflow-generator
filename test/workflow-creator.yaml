global:
  values:
  workflows:
    - deployment.yml
    - docs.yml
    - pr.yml
    - release-draft.yml
    - workflow-creator.yml

scoped:
  - name: my-app
    values:
      my_value: 'my-app'
    workflows:
      - check.yml
      - deployment-trigger.yml
